# Core
pyyaml>=6.0
typer[all]>=0.9.0
rich>=13.0.0

# LLM providers — install the ones you use
openai>=1.0.0              # Groq, OpenAI, Azure, DeepSeek (all OpenAI-compatible)
anthropic>=0.18.0          # Anthropic (Claude)
google-generativeai>=0.5.0 # Google Gemini
huggingface-hub>=0.20.0    # HuggingFace

# Embeddings (required for llm-repair strategy only)
sentence-transformers
